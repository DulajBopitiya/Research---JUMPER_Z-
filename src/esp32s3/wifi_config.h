#pragma once

// ── Wi-Fi credentials ─────────────────────────────────────────────────────────
// Edit these before flashing.  Keep this file out of git (.gitignore it).
#define WIFI_SSID "your_network_name"
#define WIFI_PASS "your_password"
